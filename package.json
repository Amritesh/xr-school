{
  "name": "xr-school",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-first K-12 XR simulation curriculum platform for Indian schools",
  "type": "module",
  "scripts": {
    "build": "npm run build:packages && npm --workspace apps/web run build",
    "build:packages": "node scripts/build-packages.mjs",
    "env:check": "node scripts/dev-env-check.mjs",
    "contract:compile": "tsp compile contracts/typespec",
    "catalog:validate": "node scripts/validate-simulation-catalog.mjs",
    "simulations:validate": "node scripts/validate-simulations.mjs",
    "narration:validate": "npm run build:packages && npm run narration:validate:manifests",
    "narration:validate:manifests": "node --import tsx scripts/validate-narration-manifests.ts",
    "narration:author": "node scripts/author-narration-assets.mjs",
    "web-catalog:generate": "node --import tsx scripts/generate-web-catalog.mjs",
    "reports:validate": "node --import tsx scripts/validate-simulation-quality-reports.ts",
    "reports:test": "python3 -m unittest discover -s tests/reporting -p 'test_*.py' -v",
    "reports:generate": "python3 scripts/generate_simulation_quality_report.py --output-dir output/pdf && python3 scripts/generate_new_simulations_top_10_mistakes.py --output-dir output/pdf && python3 scripts/generate_aditya_contribution_report.py --output-dir output/pdf",
    "reports:check": "python3 scripts/check_simulation_quality_reports.py",
    "test:e2e": "playwright test --config playwright.config.ts",
    "api:test": "npm --workspace @xr-school/api run test",
    "api:build": "npm --workspace @xr-school/api run build",
    "quality": "npm run env:check && npm run contract:compile && npm run build:packages && npm run catalog:validate && npm run narration:validate:manifests && npm run web-catalog:generate && npm run test",
    "verify": "npm run env:check && npm run contract:compile && npm run spec:drift && npm run build:packages && npm run catalog:validate && npm run web-catalog:generate && npm run simulations:validate && npm run narration:validate:manifests && npm run reports:validate && npm run test && npm run type-check:packages && npm run api:test && npm run api:build && npm --workspace apps/web run type-check && npm --workspace apps/web run build && npm run reports:test && npm run reports:check && npm run test:e2e",
    "test": "vitest run",
    "type-check:packages": "node scripts/type-check-packages.mjs",
    "spec:drift": "node scripts/spec-drift-check.mjs",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write \"**/*.{ts,tsx,json,md,yaml}\"",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@typespec/compiler": "^1.13.0",
    "@typespec/http": "^1.13.0",
    "@typespec/openapi3": "^1.13.0",
    "@typespec/rest": "^0.83.0",
    "eslint": "^9.39.4",
    "husky": "^9.1.7",
    "prettier": "^3.8.4",
    "tsx": "^4.22.4",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
